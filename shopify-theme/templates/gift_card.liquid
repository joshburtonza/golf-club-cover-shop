<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ 'gift_cards.issued.title' | t }} - {{ shop.name }}</title>
  {{ content_for_header }}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
</head>
<body style="background:var(--color-background);min-height:100vh;display:flex;align-items:center;justify-content:center;padding:2rem;">
  <div style="max-width:32rem;width:100%;text-align:center;">
    <h1 style="font-family:var(--font-display);font-size:2rem;margin-bottom:0.5rem;color:var(--color-foreground);">
      TOPPED <span style="color:var(--color-accent);">IT</span>
    </h1>
    <p style="font-family:var(--font-body);color:var(--color-muted-foreground);margin-bottom:2rem;">Gift Card</p>

    <div style="background:var(--color-card);border:1px solid var(--color-border);border-radius:0.75rem;padding:2rem;box-shadow:var(--shadow-card);">
      {%- if gift_card.image -%}
        <img src="{{ gift_card.image | image_url: width: 400 }}" alt="" style="max-width:100%;border-radius:0.5rem;margin-bottom:1.5rem;">
      {%- endif -%}

      <p style="font-family:var(--font-display);font-size:2.5rem;color:var(--color-accent);margin-bottom:0.5rem;">
        {{ gift_card.balance | money }}
      </p>

      {%- if gift_card.balance != gift_card.initial_value -%}
        <p style="font-size:0.875rem;color:var(--color-muted-foreground);margin-bottom:1rem;">
          Initial value: {{ gift_card.initial_value | money }}
        </p>
      {%- endif -%}

      <div style="background:var(--color-secondary);padding:1rem;border-radius:var(--radius);margin:1.5rem 0;">
        <p style="font-size:0.75rem;color:var(--color-muted-foreground);margin-bottom:0.5rem;">Gift card code:</p>
        <p style="font-family:monospace;font-size:1.25rem;font-weight:700;color:var(--color-foreground);letter-spacing:0.1em;">
          {{ gift_card.code | format_code }}
        </p>
      </div>

      {%- if gift_card.expired -%}
        <p style="color:var(--color-destructive);font-weight:600;">This gift card has expired.</p>
      {%- endif -%}

      {%- if gift_card.enabled -%}
        <a href="{{ shop.url }}" class="btn btn-walnut btn-lg" style="margin-top:1rem;">
          Shop Now
        </a>
      {%- endif -%}
    </div>

    <p style="font-size:0.75rem;color:var(--color-muted-foreground);margin-top:2rem;">
      &copy; {{ 'now' | date: '%Y' }} {{ shop.name }}
    </p>
  </div>
</body>
</html>
